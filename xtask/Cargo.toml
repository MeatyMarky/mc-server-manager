[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
publish = false
description = "Maintenance tasks for the repository (run via `cargo xtask <task>`)"

[dependencies]
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "blocking", "json"] }
serde_json = "1"
